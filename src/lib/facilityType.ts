/**
 * 시설 유형 코드(FCLTY_TY) → 사람이 읽을 라벨.
 *
 * ⚠️ API 명세서의 코드표를 구하지 못해, 배포된 61,334건의 쉼터 이름 분포로
 * 역추적한 값이다. 명세서를 확인하면 이 표를 정정할 것.
 *
 *   003  50,361건(82%)  이름의 86%가 '경로당'
 *   001   7,004건(11%)  행정복지센터·도서관·보건소·복지관 등 공공시설
 *   004   2,836건(5%)   은행·마트·교회 등 민간 협약 시설
 *   002   1,133건(2%)   정자·공원·그늘막 등 실외
 *
 * 매핑을 파이프라인이 아니라 앱에 둔 이유: 추론이라 틀릴 수 있고, 그때
 * 데이터를 다시 배포하지 않고 이 파일만 고쳐서 대응하기 위해서다.
 * 원본 코드는 데이터에 그대로 남아 있다.
 */
const LABELS: Record<string, string> = {
  '001': '공공시설',
  '002': '야외쉼터',
  '003': '경로당',
  '004': '민간시설',
};

/** 모르는 코드는 null을 돌려 화면에서 아예 빼버린다. '005' 같은 날코드를 보여주지 않는다. */
export function facilityTypeLabel(code: string | null): string | null {
  if (!code) return null;
  return LABELS[code] ?? null;
}
