# 이 파일을 keystore.properties로 복사해서 값을 채운다.
# keystore.properties와 *.keystore는 git에 올라가지 않는다.
#
# 키를 잃어버리면 같은 앱으로 업데이트를 낼 수 없다. 반드시 백업할 것.
# 키 생성은 README의 '릴리스 빌드' 절 참고.

# 프로젝트 루트 기준 경로
storeFile=release.keystore
storePassword=
keyAlias=coolfiltermap
keyPassword=
